import { Injectable } from '@angular/core';
import { Rating } from '../../models/rating';
import { UserService } from './user.service';

@Injectable({
  providedIn: 'root',
})
export class RatingService {
  getRatingsByVideo(): void {}
}
